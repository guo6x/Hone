{
  "name": "hone-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Hone Relay — Cloudflare Workers WebSocket relay",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.91.0"
  }
}
